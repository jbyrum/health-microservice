build:
   docker:
      web: Dockerfile

run:
   web: 
      command: 
         - gunicorn --bind 0.0.0.0:$PORT wsgi
      image: web
   worker: 
      command: 
         - gunicorn --bind $HEROKU_PRIVATE_IP:1234 wsgi
      image: web
